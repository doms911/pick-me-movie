## 2026-09-25

**Done**
- Defined PickMeMovie: an end-to-end movie and TV series recommender with
  click-based onboarding and an optional free-text request.
- Designed the architecture: frontend → agent → recommender → Postgres + pgvector,
  fed by offline pipelines (TMDB + MovieLens).
- Set up the monorepo with a uv workspace (`backend`, `pipelines`), Docker Compose
  with pgvector, README, architecture doc and ADRs 0001–0004.

**Learned**
- A two-tower model generalizes matrix factorization with features; ALS comes
  first as the baseline.
- MovieLens has no TV series; those rely on content similarity at first.
- TMDB IDs are unique per media type, so the key is `(media_type, tmdb_id)`.

**Next**
- TMDB ingest on `feat/tmdb-ingest`.